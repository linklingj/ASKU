pydantic>=2
requests>=2.31
beautifulsoup4>=4.12
protego>=0.3          # robots.txt 파서: 표준 robotparser 는 와일드카드·Allow 최장 일치를 못 다룬다
SQLAlchemy>=2.0
psycopg[binary]>=3.1
pgvector>=0.3
python-dotenv>=1.0   # 개발용 스크립트가 backend/.env 를 읽는다(운영은 컨테이너 환경변수)

# 첨부 문서 인제스트 (사용자 업로드 — app/attachment_ingest.py)
pdfplumber>=0.11      # PDF 페이지별 텍스트 추출
olefile>=0.47         # HWP 5.0(OLE) 본문 스트림 읽기. HWPX 는 표준 zipfile 로 처리

# Backend API (FastAPI)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9  # multipart 첨부 업로드(UploadFile) 처리에 필요
httpx>=0.27           # FastAPI TestClient 내부 의존

# LLM Provider 구현체별 의존 (지연 import — 해당 제공자를 쓸 때만 필요)
google-genai        # GeminiProvider: 답변 생성·구조화 추출
FlagEmbedding       # LocalEmbedder: 로컬 bge-m3 임베딩(dense)
ollama              # OllamaProvider: 로컬 답변 생성
