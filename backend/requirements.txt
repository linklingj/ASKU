pydantic>=2
requests>=2.31
beautifulsoup4>=4.12
protego>=0.3         # robots.txt 파서: 표준 robotparser 는 와일드카드·Allow 우선순위를 처리 못 함
SQLAlchemy>=2.0
psycopg[binary]>=3.1
pgvector>=0.3
pdfplumber>=0.11     # PdfIngestor: 크롤링 중 발견된 PDF 첨부파일의 페이지별 텍스트 추출(문서 RAG)

# Backend API (FastAPI)
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27           # FastAPI TestClient 내부 의존

# LLM Provider 구현체별 의존 (지연 import — 해당 제공자를 쓸 때만 필요)
google-genai        # GeminiProvider: 답변 생성·구조화 추출
FlagEmbedding       # LocalEmbedder: 로컬 bge-m3 임베딩(dense)
ollama              # OllamaProvider: 로컬 답변 생성
